import 'package:flutter/material.dart';

class UIConstants {
  static const double defaultPadding = 24.0;
  static const double largePadding = 32.0;
  static const double defaultSpacing = 16.0;
  static const double smallSpacing = 8.0;

  static const double maxFormWidth = 400.0;
  static const double loadingIndicatorSize = 20.0;

  static const EdgeInsets screenPadding = EdgeInsets.all(defaultPadding);
  static const EdgeInsets cardPadding = EdgeInsets.all(largePadding);

  static const double buttonSpacing = 16.0;
}
